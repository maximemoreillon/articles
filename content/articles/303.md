+++
title = "Docker restart container when docker restarts"
date = '2020-02-01'
lastmod = '2020-03-10'
tags = ['Tutorials', 'Docker']
+++

Simply add the following flag when using docker run

```
  --restart=always
```

If the container is already running:

```
docker update --restart=always <container>
```