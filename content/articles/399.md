+++
title = "Bash check if environment variable is set"
date = '2020-04-23'
lastmod = '2020-05-10'
tags = ['Bash']
+++

When writing bash scripts, it is sometimes necessary to check if an environment bariable is set.

The following command will print `hello` if the environment variable `HELLO` is set:

```
[[ -z ${HELLO}]] || echo "hello"
```