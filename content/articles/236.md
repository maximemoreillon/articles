---
title: "Tensorflow 1.X basics"
date: '2020-03-18'
lastmod: '2020-03-19'
tags: ['Python', 'AI / ML', 'TensorFlow', 'Tutorials']
---

Here are the basics steps necessary to write TensorFlow code.

Note that this is for **TensorFlow 1.X**

## Define constants

```python
a = tf.constant(5.0)
```

## Define variables

```python
x = tf.Variable(init_value)
```

## Define placeholders

```python
p = tf.placeholder(tf.float32)
```

## Define operations

```python
c = x * x
```

## Define way to measure loss

```python
loss=tf.reduce_mean(tf.square(output - y))
```

## Define optimizer

```python
optimizer = tf.train.AdamOptimizer(learning_rate)
```

## Apply optimizer to loss function

```python
train = optimizer.minimize(loss)
```

## init variables

```python
init = tf.global_variables_initializer()
```

## Run everything in a session

```python
with tf.Session() as sess:
	sess.run(init)
	
	for epoch in range(epoch_count):
		sess.run(train, feed_dict={<feedDictHere})
```